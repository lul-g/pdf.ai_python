# To Run app 
- ## Create venv
  - `python -m venv ./`
- ## activate venv
  - `source bin/activate`
- ## install libs
  - `pip install -r requirements.txt`
- ## run app
  - `streamlit run app.py`
 
# `Make sure to have openAi secret in .env file `
